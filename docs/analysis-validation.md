# 讨论分析验收记录

本文件记录讨论分析的开发与 0.4.2 预发布验证证据，不代表商店已更新或真实模型已经完成准入。更新日期：2026-09-17。

## 2026-09-17 0.4.2 预发布验证

- 本版包括此前 0.4.0 分析工作区、0.4.1 本帖侧栏，以及 0.4.2 B 站图床接入；公开试用不等于正式标准分析准入。
- 最终本地回归：47 个文件、590 项通过，类型检查和打包通过。公开 CI 缺少整帖原件时跳过 1 项完整本地样板测试，不影响其余随仓库提供的回归样例。
- 最终安装包、SHA-256、权限及排除文件检查见 [验证范围](verification.md)。原始采集原页、完整讨论样板和私有操作记录继续排除；公开测试只含必要的原帖摘录与明确标注的合成材料。
- 下方 0.4.0、0.4.1 的包名、哈希及当时发布状态均为历史记录；当前下载使用 [v0.4.2 Releases](https://github.com/laoertongxue/GuoZaoKe-Polish/releases/tag/v0.4.2)。
- 仍未进行真实 DeepSeek 调用或实际多模型资格验证；不会因此次推送更改未完成验收项。

## 2026-09-17 本帖侧栏修复

- 原入口挂载只读预览，只读取一次历史，不能启动任务；报告概览也只有数量、问题和缺口，缺少具体主张与回复结论。已改为用户点击后打开原生 Chrome 扩展侧栏，加载同一套受信工作区；站点页面不能调用凭据与模型代理接口，受信 `analysis.html` 不加入网页可访问资源。
- 侧栏恢复本帖最新报告及运行检查点。没有报告且会话 Key 可用时，仅自动采集供用户检查；模型请求仍需发送确认和主动开始。Key 缺失、失败与中断分别提示，完成报告不会重复执行。同一任务通过 Web Locks 限制为一个执行窗口。
- 概览直接展示主张、材料支持/冲突理由、来源与限制、原文定位及回复各维度判断；系统补查与原回复举证分开，NA/P/U 不变成低分，不添加个人总分。
- 本地浏览器流程：合成材料→合成帖采集→开始→取消→重载恢复→将调用上限改为 8→主动继续→主张/关系/回复逐阶段呈现→6/6 步完成→再次重载直接恢复报告，无新增模拟请求。最终占用 5/8 次，包含取消前已占用的 1 次；真实 API 请求为 0。该流程发现并修复预算输入依赖失焦的问题。
- 在 Codex 内置浏览器的 390×844 视口，浅色主张结论与深色回复诊断均可读，页面宽度与滚动宽度均为 390；随后恢复视口。报告 ID 为 `6d2f91bb-0c2c-49ff-9430-b9a2f9e8d993`。这验证的是生产工作区与执行器在本地夹具中的行为，不是已安装 Chrome 原生侧栏或 DeepSeek 实际响应。
- 用户已说明配置 DeepSeek。浏览器工具不允许操作扩展页面，本轮没有读取 Key、没有绕过该限制，也没有实际调用 DeepSeek。原生侧栏打开、现有会话配置复用及真实帖子语义结果仍需已安装扩展验收，不能将本轮测试视为标准模式准入。
- 新增侧栏/恢复/结论/预算/并发回归；全量回归发现布局入口遇到同步消息失败时会中断，已用 async try/catch 覆盖同步失效与异步失败，点击事件仍在任何 await 之前发送打开请求。
- 独立复审发现历史重开可能留下未保存的高预算编辑值，已先用回归复现，再删除冗余编辑状态；执行时冻结用户当前看到的上限，取回最新累计调用账本后只应用该上限。最终全量回归 47 个文件、577 项通过（15:28），类型检查通过，`git diff --check` 通过。
- 修复后独立复审确认该 P2 关闭、没有新增阻断问题，并独立复跑侧栏 17 项测试全部通过。

### 历史构建：0.4.1

- `.output/guozaoke-polish-0.4.1-chrome.zip`，808848 字节、31 个条目，2026-09-17 重新构建与打包。
- SHA-256：`d0bffb697af7e738d3881f9e12552cb721aae22ca3d20379247bb450defb0a3b`。
- 检查根 manifest 版本 0.4.1、`sidePanel` 权限、侧栏准备/打开路由、同任务执行锁及新结论文案；受信 `analysis.html` 未列入网页可访问资源。归档完整性通过，没有测试目录、研究材料、source map、`.env` 或 `.git`；凭据形态扫描未匹配。回执位于本地 `artifacts/analysis-sidebar-20260917/build-receipt.json`。
- 该次 0.4.1 构建时未提交或推送 GitHub、未更新 Chrome 应用商店；0.4.2 发布情况见本文开头和 Releases。

## 已取得的验证

- 最终全量：`npm test -- --maxWorkers=4`，46 个文件、560 项通过（2026-09-16 09:12）；`npm run typecheck` 通过；构建包记录见下方。
- 删除记录先保留 ID 删除标记，拒绝晚到检查点，同源共享写入锁串行化保存与删除，避免旧快照恢复已删内容或覆盖其他报告；相关回归由失败转为通过。
- 自动取证有累计字节与尝试次数账本；任务缺失不降级为手工读取，账本丢失不重新授予额度。逐问题检索在发送前复查绝对期限，保存进度跨截止时不发送。
- 命令行校准默认预览不读 Key、不联网、不写文件；执行时保留全部配置/样例矩阵与失败槽位，所有配置共享调用预算。入口、模拟 HTTP、取消及写入失败已测，没有实际 API 结果。
- 真实帖 121894：26 条发言、68 项主张可逐项定位回原文，完整本地样板测试实际执行。8 个来源只有线索/不可读记录，7 个旧数值没有无依据迁入。见 `scripts/analysis-import-reference.mjs` 与 `tests/analysis-real-thread.test.ts`。
- 2026-09-15 18:40–18:44：匿名读取首页后先固定 10 个新帖，保留失败项，再显式增加 3 帖。13 个尝试中 9 个采集成功，得到 104 条发言（主楼 9 条、回复 95 条）和 258 个 Unicode 原文片段；稳定回复 ID 与原页 ID 集合一致，片段完整重组为采集文字，离线复放的 9 个快照指纹全部一致。4 帖含图片，均标记部分覆盖。4 个读取失败不算空帖或完整采集，其中 133230、132937 确认为登录跳转；另两项保留 fetch failed。实际读取成功的都是单页，本次没有新增真实多页通过证据。
- 上述采集材料及原页只保存在忽略提交的 `artifacts/analysis-live-capture-20260915/`；有固定样本清单、追加理由、首次失败记录、采集和离线复放回执。它们包含公开帖子原文，不能视为完全匿名的数据集；没有向模型发送、没有产生主张或评级、没有作为专家金标，也没有进入安装包。采集/真实样板相关 2 个测试文件、12 项于 18:44 重新执行通过。
- 实际来源：通过生产 `fetchEvidenceBytes` 匿名读取国家统计局公开 HTML（1,118,849 字节）和文字 PDF（2,289,767 字节）；NIST PDF 返回 HTTP 错误并保留失败。Chrome 中用生产 `importEvidenceFile` 和本地 PDF Worker 复放保存原件，字节哈希一致、重复解析正文一致；HTML 保留表格结构和明确的 PubDate 日历日期，PDF 读取 1/1 页并显示多栏顺序限制。两者 `data=[]`，没有自动变为核验事实。网络读取和浏览器本地解析分开验证，未冒充安装扩展的权限/跨域全链路。见本地 `artifacts/analysis-live-evidence-20260915/README.md`。
- 原件验证暴露并修复表格单元格段落拆散问题；独立复审再发现的嵌套表结束边界、带空格的合并属性、空表/图片表误判可读、表内登录墙漏检均先复现失败后修复。规格和质量复审均通过，另有 78 项相关回归及独立本地复现。工具流程指纹已更新为 `trial-source-structure-v4`，旧准入不能沿用。
- 实际 Chrome 布局检查：390 像素 iframe 内，深色报告/来源展开、浅色摘录复核表单分别检查，`clientWidth=390`、`scrollWidth=390`；原文定位、校验说明、未知值和重新采集入口可见。运行的是本机示例夹具，不是已安装扩展或真实 API 验收。
- 2026-09-16 本地 Chrome 流程夹具：补充合成材料→采集合成帖子→主动开始→保存主张→切到模型设置取消→确认后继续→展开摘录→完成→主动刷新→重载后从历史找回。真实工作区、采集器、执行器和仓库参与运行；模型响应、权限和帖子加载由明确标注的夹具提供，没有实际 API 请求。模拟阶段次数为 claims 1、plan 2（含已取消的首次请求）、relations 1、replies 1，5 次占用保留；最终 6/6 步保存，报告仍显示待核事项。390px 真实视口下浅色来源面板及深色来源正文的页面宽度/滚动宽度均为 390px，之后恢复浏览器视口。详见本地 `artifacts/analysis-workflow-20260916/README.md`。
- 上述流程补齐跨页进度/取消、阶段结果更新、完成态禁用重复执行；独立质量审查发现并修复普通完成、延迟准入返回、复跑队列结束和自动下一轮开始时的展开/焦点丢失。6 项新用例先失败后通过，两个工作区文件共 15 项通过，独立复审 PASS。展开原文时保留当前显示及导出版本，由用户主动刷新；这不替代真实模型或安装扩展验收。

- 删除/预算/问题计划/准入/摘录复核/CLI 的本轮规格与质量复审均通过；共享写入锁另以 Chrome 页面与 module Worker 的实际 `navigator.locks` 执行验证，A/B/C 并发保存均保留，删除 A 与保存 D 后 B/C/D 保留，旧 A 写入拒绝。该验证使用合成内存存储，不等同安装扩展或真实 API 验收。相关跨上下文语义见 [Web Locks 文档](https://developer.mozilla.org/en-US/docs/Web/API/Web_Locks_API)。

## 按产品方案核对

| 方案要求 | 实现与测试入口 | 当前结论及未完成证据 |
| --- | --- | --- |
| 1 单帖分析、个人 API、本地保存、不建立公开画像 | `types.ts`、`providers.ts`、`repository.ts`、`workspace.ts`；providers/repository/workspace 测试 | 代码与模拟测试成立；未调用真实服务 |
| 2 配置→采集→主动发送→分阶段→回看→更新 | `snapshot.ts`、`engine.ts`、`workspace.ts`、`features/analysis.ts`；snapshot/engine/workspace 测试 | 路径及指出摘录错误→重新采集新版本入口已实现，旧报告保留且不自动发送；实际安装 Chrome 全流程未完成 |
| 3 概览、主张与证据、回复观察、设置历史 | `workspace.ts`、`analysis.css`；workspace 测试与浏览器夹具 | 基本页面已实现；真实大帖下的完整交互与各主题仍需验收 |
| 4 采集、原文定位、主张、计划、取证、关系、分段评价、报告 | `snapshot.ts`、`contracts.ts`、`prompts.ts`、`engine.ts`、`evidence.ts` | 结构、精确摘录、提供者归属与状态检查有回归；主张语义、指标/人群/时期适用性不能以结构检查替代。逐问题目标口径、资料类型、正反方向、绝对截止时间、次数预算和停止条件已冻结；旧材料显式 plan:null，不伪造计划 |
| 4 数值原值、单位与程序换算 | `contracts.ts`、`conversions.ts`；contracts/conversions 测试 | 原值及紧邻单位通过后进入数据表，其余待核；固定尺度换算可复算。表格跨单元格的语义配对未自动验证 |
| 5.1 同快照抽取、同材料评价两轨对比 | `comparison.ts`、`workspace.ts`；comparison 测试 | 输入指纹不同时禁止直接计算一致率；自然语言变动保留待核；实际多配置比较未执行 |
| 5.2 可复用分析包、不含 Key、导入不代表可信 | `contracts.ts`、`repository.ts`、后台 report qualification 查询；contracts/repository/background 测试 | 结构、引用、完整性、换算重算已测；导入 standard 标记不能取得本机验证；哈希不证明事实真实 |
| 5.3 精确配置准入、参数/路由变化失效、试用标识 | `calibration.ts`、`comparison.ts`、`background.ts`、`workspace.ts` | 21 合成样例×3 次机制；普通任务准入与漂移失效、发送前配置竞态已通过规格及质量复审；没有实际配置通过记录 |
| 6 双向检索、读取原件、手工 URL/文件、同源分组与缺口 | `retrieval.ts`、`evidence.ts`、后台权限代理；evidence/retrieval 测试 | 原页/文字 PDF 已有实际下载与 Chrome 本地解析证据；表格结构及不可读判断修复通过复审。安装扩展权限链路、双向检索服务与外部来源语义适用性仍需验证 |
| 7 总预算、取消、恢复、更新与部分完成 | `engine.ts`、`calibration.ts`、`repository.ts`、`workspace.ts` | 预算在发送前占用，部分结果保存，配置冻结回归通过；自动原件读取有 24 MiB 默认累计额度与来源上限×3 次尝试，失败保留占用；手工读取明确独立，任务恢复不重置自动额度 |
| 8 会话 Key、精确目的域、消息隔离、无生成 HTML | `providers.ts`、`background.ts`、`repository.ts`、`entrypoints/background.ts` | 安全边界模拟回归；会话存储不入同步/导出；安装包检查见下方构建记录，不能承诺已控制设备下的秘密性 |
| 9 A 冻结输入复跑工作台 | 实际样板适配、两轨执行/对比及校准工作区 | 软件与离线测试可用；3 实际配置×3 次运行未执行 |
| 9 B 新帖完整流程 | 采集及分阶段执行器 | 新帖匿名采集尝试 13 个、成功 9 个并离线复放；4 个失败保留，图片限制明确。至少 10 个新真实帖的实际模型/证据验证仍缺失 |
| 9 C 扩展试用体验 | 主题页侧栏、独立页面、控制选项入口 | 实际安装的配置→分析→来源→更新→恢复/取消流程未验证；浏览器工具不能操作扩展管理页面，需用户手动加载 |
| 9 D 标准模式开放 | 固定合成试验集、后台回执、准入计算与版本失效 | 试验机制已实现，正式开放条件未达成；没有独立人工专家金标、真实多模型结果或新帖语义评估 |
| 10 规格、计划、服务边界、错误、差异报告交付 | 方案及本地实现计划、`docs/analysis.md`、双语隐私、测试与工作区 | 扩展校准工作区与独立 `analysis-calibrate.mjs` CLI 均已交付源码，CLI 产物不能赋予扩展受信资格；真实校准仍待执行 |

## 仍需实际完成的验收

1. 用户在 Chrome 手动加载 `.output/chrome-mv3`，在扩展内填写自有配置及 Key，并给出实际调用预算；不要把 Key 发进聊天。
2. 使用至少 3 个明确配置，每配置 3 次运行，至少 10 个新真实帖和冻结留出片段。分别统计抽取、引用支持、适用性、弃判、推导及重复稳定性；不把模型多数票当真值。
3. 完成已安装扩展的全链路交互、主题和窄屏验证，验证结果及材料真正持久化。
4. 补齐上表明确缺口，完成逐项规格与质量审查，再判断目标完成。产品验收目标尚未完成，需在已安装扩展中配置真实模型及调用预算。0.4.2 按试用范围公开预发布，不能以代码发布替代上述验收；Chrome 应用商店需另行提交。

## 历史构建：0.4.0

- 文件：`.output/guozaoke-polish-0.4.0-chrome.zip`。
- 版本：0.4.0 开发试用版；806492 字节，31 个条目。2026-09-16 全量回归后，`npm run zip` 重新完成生产构建及压缩。
- SHA-256：`8884e8319a798850cfdb8e3d6fd61a0e571d0b91cddb78c97416a67fd677ddfe`。
- 已检查根目录 manifest、分析页与只读预览页、PDF worker/许可证、最新任务进度/完成按钮和 v4 流程指纹；未带入测试目录、流程夹具、研究材料、source map、`.env` 或 `.git`；凭据形态扫描无匹配。检查脚本处理构建产物中的 Unicode 转义后确认新文案存在。归档完整性检查通过，扫描不替代真实服务验证。当前回执保存在本地 `artifacts/analysis-workflow-20260916/build-receipt.json`。
- 该次 0.4.0 构建仅在隔离开发目录生成，当时未提交或推送 GitHub、未更新 Chrome 应用商店。
